[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

# Railway injects env vars automatically - no need to declare them here

[start]
cmd = "npx serve dist -s -l ${PORT:-3000}"
