[build]
builder = "nixpacks"
buildCommand = "echo 'API Key exists:' && [ -n \"$VITE_ALCHEMY_API_KEY\" ] && echo 'Yes' || echo 'No' && npm install && npm run build"

[deploy]
startCommand = "npx serve dist -s -l 3000"

[env]
NODE_ENV = "production"
